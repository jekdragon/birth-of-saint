# Birth of the Saint — План фиксов по Code Review
> 15 пунктов | Порядок: приоритетный (критические → чистка)

---

## BUG-1: spawn_damage передаёт player вместо is_crit
**Файлы:** weapons.py
**Время:** 5 мин
**Риск:** низкий (изолированная замена аргумента)

### Шаги:
1. `weapons.py:298` — заменить `player` на `is_crit`:
   ```
   - floating_numbers.spawn_damage(e.pos.x, e.pos.y, actual_dmg, d["color"], player)
   + floating_numbers.spawn_damage(e.pos.x, e.pos.y, actual_dmg, d["color"], is_crit)
   ```
2. `weapons.py:575` — заменить `_player` на `_is_crit`:
   ```
   - _floating.spawn_damage(e.pos.x, e.pos.y, actual_dmg, _color, _player)
   + _floating.spawn_damage(e.pos.x, e.pos.y, actual_dmg, _color, _is_crit)
   ```
3. Проверить: `python tests/test_phase25.py` — 788/788
4. Проверить визуально: обычные удары = белые, криты = жёлтые

### Депенденси:
- weapons.py → projectiles.py (FloatingNumberManager.spawn_damage)
- main.py (вызывает weapon.update с damage_numbers_unused)

---

## BUG-2: CharSelectScene / StageSelectScene — сломан API
**Файлы:** char_select.py, stage_select.py
**Время:** 15 мин
**Риск:** средний (меняет интерфейс двух классов)

### Шаги:
1. `char_select.py` — добавить наследование:
   ```python
   from scene_manager import Scene
   
   class CharSelectScene(Scene):
   ```
2. `char_select.py` — переименовать `handle_event` → `handle_events`, обернуть логику в цикл:
   ```python
   def handle_events(self, events):
       for event in events:
           result = self._handle_single(event)
           if result:
               return result
       return None
   
   def _handle_single(self, event):
       # существующая логика handle_event
   ```
3. Аналогично для `stage_select.py`
4. Добавить `enter(**kwargs)` и `draw(screen)` совместимость с Scene
5. `python tests/test_phase25.py` — 788/788
6. Проверить ручной переход: title → char_select → stage_select → game

### Депенденси:
- scene_manager.py (базовый Scene)
- main.py (регистрация сцен)
- menu.py (может вызывать char_select через MainMenu flow)

---

## QUAL-1: 47 unused импортов
**Файлы:** 16 файлов
**Время:** 10 мин
**Риск:** низкий (удаление неиспользуемого кода)

### Шаги:
1. Удалить unused импорты по файлам:
   - `config.py` — `import math`
   - `bestiary.py` — `RED, GREEN, PARCH_DARK, PARCH_MID, PARCH_BASE, PARCH_LIGHT`
   - `cathedral.py` — `import random, MAP_WIDTH, MAP_HEIGHT, TILE_SIZE`
   - `effects.py` — `MAP_WIDTH, MAP_HEIGHT`
   - `enemies.py` — `WHITE`
   - `game_over_screen.py` — `DARK_BG, WEAPON_DEFS, generate_parchment, PARCH_INK, PARCH_INK_DIM, PARCH_BASE, PARCH_LIGHT`
   - `main.py` — `DamageNumber, Particle, pygame.math, ENEMY_TYPES, PauseOverlay, _tb, CATHEDRAL_COLORS`
   - `menu.py` — `DARK_BG, RED`
   - `music.py` — `import random`
   - `obstacles.py` — `MAP_WIDTH, MAP_HEIGHT, TILE_SIZE`
   - `relics.py` — `GREEN`
   - `scenes.py` — `RED`
   - `stage_select.py` — `GREEN`
   - `wave_manager.py` — `CENTER_X, CENTER_Y`
   - `weapons.py` — `calc_damage_mult, calc_cooldown_mult, calc_area_mult, RUNE_DEFS, Pulse, make_damage_number, DamageNumber, Particle`
2. Удалить строку-комментарий `weapons.py:779` (`# Нужен для DamageNumber и Particle`)
3. `python tests/test_phase25.py` — 788/788
4. `python -c "import main"` — нет ImportError

### Депенденси:
- Нет (чистое удаление)

---

## QUAL-2: damage_numbers_unused — мёртвый параметр
**Файлы:** weapons.py, main.py
**Время:** 10 мин
**Риск:** средний (меняет сигнатуру 10 методов + все вызовы)

### Шаги:
1. `weapons.py` — удалить `damage_numbers_unused` из сигнатуры всех 10 weapon update():
   ```python
   # Было:
   def update(self, player, enemies, projectiles, pulses, particles, damage_numbers_unused, dt):
   # Стало:
   def update(self, player, enemies, projectiles, pulses, particles, dt):
   ```
2. `main.py` — обновить вызов weapon.update(), убрать floating_numbers из аргумента:
   ```python
   # Было:
   w.update(self.player, self.enemies, self.projectiles, self.pulses, self.particles, floating_numbers, dt)
   # Стало:
   w.update(self.player, self.enemies, self.projectiles, self.pulses, self.particles, dt)
   ```
3. `python tests/test_phase25.py` — 788/788

### Депенденси:
- main.py (вызовы weapon.update)
- weapons.py (10 классов оружия)

---

## PERF-3: random.seed() ломает глобальное состояние
**Файлы:** menu.py
**Время:** 5 мин
**Риск:** низкий

### Шаги:
1. `menu.py` — добавить локальный генератор в `__init__`:
   ```python
   self._rng = random.Random(42)
   ```
2. `_generate_stone_texture()` — заменить `random.seed(42)` + `random.randint` на `self._rng.randint`:
   ```python
   # Было:
   random.seed(42)
   ...
   n = random.randint(-15, 15)
   # Стало:
   self._rng.seed(42)  # или просто используем self._rng
   ...
   n = self._rng.randint(-15, 15)
   ```
3. `draw_main()` кнопки — заменить `random.seed(i*37)` на отдельный rng:
   ```python
   btn_rng = random.Random(i * 37)
   ```
4. `python tests/test_phase25.py` — 788/788

### Депенденси:
- Нет (изолировано в menu.py)

---

## ARCH-5: STAGES не синхронизированы с MAPS
**Файлы:** config.py, menu.py, stage_select.py
**Время:** 15 мин
**Риск:** средний (меняет источник данных для двух экранов)

### Шаги:
1. `config.py` — добавить единый dict карт:
   ```python
   MAP_DEFS = {
       "arena":     {"name": "Арена",     "desc": "Бесконечная равнина, 4 биома-кольца", "diff": 2, "bonus": "+10% скорость", "unlocked": True},
       "cathedral": {"name": "Собор",     "desc": "Узкие коридоры, залы, колонны",       "diff": 3, "bonus": "+15% золото",   "unlocked": True},
       "catacombs": {"name": "Катакомбы", "desc": "Подземелья под собором. Темнота и ужас.", "diff": 3, "bonus": "", "unlocked": False},
       "hellgate":  {"name": "Врата Ада", "desc": "Портал в преисподнюю. Финальное испытание.", "diff": 5, "bonus": "", "unlocked": False},
   }
   MAP_ORDER = ["arena", "cathedral", "catacombs", "hellgate"]
   ```
2. `menu.py` — удалить локальные `MAPS` и `MAP_ORDER`, импортировать из config:
   ```python
   from config import MAP_DEFS as MAPS, MAP_ORDER
   ```
3. `stage_select.py` — удалить локальные `STAGES`, импортировать из config:
   ```python
   from config import MAP_DEFS, MAP_ORDER
   ```
   Обновить все ссылки `STAGES` → `MAP_DEFS.values()` или аналог.
4. `python tests/test_phase25.py` — 788/788

### Депенденси:
- config.py (добавление MAP_DEFS)
- menu.py (удаление MAPS, импорт)
- stage_select.py (удаление STAGES, импорт)
- main.py (проверить что selected_map работает)

---

## PERF-1: Шрифты пересоздаются каждый кадр
**Файлы:** char_select.py, stage_select.py, game_over_screen.py, confirm_dialog.py
**Время:** 10 мин
**Риск:** низкий

### Шаги:
1. В каждом файле добавить модульный кэш (как в scenes.py):
   ```python
   _font_cache = {}
   def _get_fonts():
       if not _font_cache:
           _font_cache['font'] = pygame.font.Font(None, 24)
           _font_cache['big'] = pygame.font.Font(None, 48)
           _font_cache['small'] = pygame.font.Font(None, 18)
       return _font_cache['font'], _font_cache['big'], _font_cache['small']
   ```
2. Заменить `draw()`:
   - `char_select.py:56-58` → `font, big_font, small_font = _get_fonts()`
   - `stage_select.py:67-69` → аналогично
   - `game_over_screen.py:246-250` → аналогично (в draw_game_over)
   - `confirm_dialog.py:73-75` → аналогично
3. Для confirm_dialog — кэшировать в __init__ или использовать переданные шрифты
4. `python tests/test_phase25.py` — 788/788

### Депенденси:
- Нет (изолировано в каждом файле)

---

## ARCH-3: 38 inline `import sound_manager`
**Файлы:** lobby.py, menu.py, scenes.py, confirm_dialog.py
**Время:** 10 мин
**Риск:** низкий (sound_manager не вызывает циклический импорт)

### Шаги:
1. Проверить что top-level import не вызывает циклический импорт:
   ```python
   # sound_manager.py не импортирует ничего из lobby/menu/scenes
   ```
2. Добавить top-level imports:
   - `lobby.py` — `import sound_manager` (удалить 22 inline)
   - `menu.py` — `import sound_manager` (удалить 7 inline)
   - `scenes.py` — `import sound_manager` (удалить 4 inline)
   - `confirm_dialog.py` — `import sound_manager` (удалить 4 inline)
3. Найти-заменить: `import sound_manager\n                ` → `` (с учётом отступов)
4. `python tests/test_phase25.py` — 788/788
5. `python -c "import main"` — нет ImportError

### Депенденси:
- sound_manager.py (проверить что не импортирует ничего циклического)

---

## ARCH-4: Несогласованные сигнатуры handle_event/handle_events
**Файлы:** scene_manager.py, char_select.py, stage_select.py, menu.py, lobby.py, bestiary.py, confirm_dialog.py
**Время:** 20 мин
**Риск:** средний (меняет интерфейс 7+ классов)

### Шаги:
1. Выбрать подход: **SceneManager адаптирует** (менее инвазивно):
   ```python
   # scene_manager.py — в handle_events():
   if self.current:
       scene = self.scenes[self.current]
       if hasattr(scene, 'handle_events'):
           result = scene.handle_events(events)
       else:
           # Адаптер: вызываем handle_event для каждого события
           result = None
           for event in events:
               result = scene.handle_event(event)
               if result:
                   break
   ```
2. Альтернатива (чистая): привести все к `handle_events(list)` — больше работы, чище архитектура
3. Рекомендация: подход 1 (адаптер) — минимальные изменения, обратная совместимость
4. `python tests/test_phase25.py` — 788/788

### Депенденси:
- scene_manager.py (добавление адаптера)
- Все сцены (проверить совместимость)

---

## ARCH-2: Два параллельных flow для выбора персонажа/карты
**Файлы:** char_select.py, stage_select.py, main.py, scenes.py
**Время:** 30 мин
**Риск:** высокий (удаление/переписывание целых файлов)

### Шаги:
1. Определить canonical flow: **Lobby flow** (LobbyScene → RunPrepScene → GameScene)
2. Если CharSelectScene/StageSelectScene не нужны:
   - Удалить регистрацию в `main.py:889-890`
   - Удалить импорты в `main.py:38-39`
   - Оставить файлы (не удалять) как legacy, добавить комментарий
3. Если нужны (альтернатива):
   - Исправить BUG-2 (наследование от Scene)
   - Добавить переход из lobby: lobby → char_select → stage_select → run_prep → game
4. Рекомендация: пока оставить как есть, удалить регистрацию из SceneManager (prevents crash)
5. `python tests/test_phase25.py` — 788/788

### Депенденси:
- main.py (регистрация сцен)
- scene_manager.py (BUG-2 должен быть исправлен первым)

---

## PERF-2: Surface создаётся каждый кадр без кэша
**Файлы:** menu.py, scenes.py, splash.py
**Время:** 30 мин
**Риск:** средний (меняет логику рендеринга)

### Шаги:
1. `menu.py` — кэшировать:
   - `_draw_vignette()` → `_vignette_cache` (обновлять только при resize)
   - Кнопки stone slab → `_button_cache[i]` (обновлять при hover/selection change)
   - Gold glow → вычислять один раз, масштабировать
2. `scenes.py` PauseOverlay — кэшировать:
   - `generate_wood_slats()` → уже кэшируется ✅
   - Арка → `_arch_cache` (статична)
   - Таблички → `_tablet_cache[selected]` (обновлять при смене selection)
3. `splash.py` — частицы пересоздаются каждый кадр (по дизайну), но fade overlay можно кэшировать
4. Добавить dirty-флаг: пересоздавать кэш только при изменении selection/resize
5. `python tests/test_phase25.py` — 788/788
6. Замерить FPS до/после

### Депенденси:
- menu.py, scenes.py, splash.py (независимые изменения)

---

## PERF-4: pygame.time.get_ticks() в headless
**Файлы:** game_over_screen.py
**Время:** 3 мин
**Риск:** низкий

### Шаги:
1. `game_over_screen.py:422` — заменить:
   ```python
   # Было:
   pulse = (math.sin(pygame.time.get_ticks() / 400.0) + 1.0) / 2.0
   # Стало:
   pulse = (math.sin(animator.timer * 2.5) + 1.0) / 2.0  # ~400ms period
   ```
2. `python tests/test_phase25.py` — 788/788

### Депенденси:
- game_over_screen.py (animator передаётся в draw_game_over)

---

## ARCH-6: Settings не применяются
**Файлы:** scenes.py (SettingsScene)
**Время:** 20 мин
**Риск:** средний (pygame mixer/display API)

### Шаги:
1. Добавить метод `apply()` в SettingsScene:
   ```python
   def apply(self):
       import sound_manager
       sound_manager.set_volume(self.volume / 10.0)
       if self.fullscreen:
           # pygame.display.toggle_fullscreen() — осторожно с WASM
           pass
   ```
2. Вызывать `apply()` при изменении значения (Left/Right/Enter)
3. Вызывать `apply()` при `enter()` (восстановить сохранённые настройки)
4. Сохранять настройки в save_system (опционально)
5. `python tests/test_phase25.py` — 788/788
6. Проверить: изменить громкость → звук изменился

### Депенденси:
- scenes.py (SettingsScene)
- sound_manager.py (set_volume)
- save_system.py (опционально, для сохранения)

---

## ARCH-7: PauseOverlay — 190 строк в draw()
**Файлы:** scenes.py
**Время:** 20 мин
**Риск:** низкий (рефакторинг без изменения поведения)

### Шаги:
1. Вынести из `PauseOverlay.draw()` в helper-методы:
   ```python
   def _draw_arch_frame(self, screen):      # ~30 строк → отдельный метод
   def _draw_title_plaque(self, screen):     # ~15 строк
   def _draw_stats_panel(self, screen):      # ~30 строк
   def _draw_tablets(self, screen):          # ~15 строк
   def _draw_candles(self, screen):          # ~20 строк
   def _draw_hint(self, screen):             # ~5 строк
   ```
2. Кэшировать арку (`_arch_cache`) — она не меняется
3. Кэшировать wood slats — уже кэшируется через `generate_wood_slats()`
4. `draw()` остаётся ~20 строк: вызовы helper'ов + confirm dialog
5. `python tests/test_phase25.py` — 788/788

### Депенденси:
- scenes.py (только внутренний рефакторинг)

---

## СВОДКА: Зависимости между фиксами

```
BUG-1 ───────────────────────────────────── (независимо)
BUG-2 ──── ARCH-2 ───────────────────────── (BUG-2 первым)
QUAL-1 ──────────────────────────────────── (независимо)
QUAL-2 ──────────────────────────────────── (после QUAL-1)
PERF-1 ──────────────────────────────────── (независимо)
PERF-2 ──────────────────────────────────── (независимо)
PERF-3 ──────────────────────────────────── (независимо)
PERF-4 ──────────────────────────────────── (независимо)
ARCH-1 ──────────────────────────────────── (часть QUAL-1)
ARCH-3 ──────────────────────────────────── (независимо)
ARCH-4 ──── после BUG-2 ─────────────────── (BUG-2 первым)
ARCH-5 ──────────────────────────────────── (независимо)
ARCH-6 ──────────────────────────────────── (независимо)
ARCH-7 ──────────────────────────────────── (независимо)
```

## ОЦЕНКА ОБЩЕГО ВРЕМЕНИ

| Группа | Время |
|--------|-------|
| Критические (BUG-1, BUG-2) | 20 мин |
| Качество (QUAL-1, QUAL-2) | 20 мин |
| Производительность (PERF-1-4) | 50 мин |
| Архитектурные (ARCH-1-7) | 115 мин |
| **Итого** | **~3.5 часа** |
